var first = window.prompt("Please enter your name:");
document.write("Welcome to University of Plymouth, " + first + ".");

/* here I'm declaring a variable called "first",
which will be populated by the value user enter into the window prompt box
next, I'm using the document.write method to display a welcome string,
which includes a comma and a space at the end, plus the value in the "first"
variable plus a full stop. */
